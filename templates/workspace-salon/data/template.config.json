{
  "template": "salon",
  "description": "Variables the agent walks the owner through on first admin chat. The agent collects answers then calls apply_template_variables to substitute them across files_to_substitute. Mark complete by overwriting this file with { completed: true, completed_at: ... }.",
  "auto_fill_from_workspace_name": ["SALON_NAME", "SALON_SHORT", "AGENT_NAME"],
  "variables": [
    { "key": "SPECIALTIES", "prompt": "What kinds of services do you offer? (e.g. 'Hair, Color, Nails', 'Men's grooming', 'Bridal')", "required": true },
    { "key": "REGION", "prompt": "Country or city you operate in (e.g. 'UAE', 'London')", "required": true },
    { "key": "CURRENCY", "prompt": "Currency code (3 letters: AED, USD, EUR, GBP...)", "required": true, "validate": "^[A-Z]{3}$" },
    { "key": "HOURS_DAILY", "prompt": "Daily operating hours (e.g. '9:00 AM – 8:00 PM')", "required": true },
    { "key": "PHONE", "prompt": "Booking phone number (with country code)", "required": true },
    { "key": "SIGN_OFF", "prompt": "Closing line in the agent's voice (e.g. 'See you soon ✨')", "default": "Thanks for choosing us ✨" }
  ],
  "files_to_substitute": [
    "skills/aaas/SKILL.md",
    "SOUL.md",
    "data/salon.txt",
    "data/background.txt"
  ]
}
